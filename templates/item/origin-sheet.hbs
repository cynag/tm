<form class="{{cssClass}}" autocomplete="off">
  <header class="sheet-header">
    <h1><input name="name" type="text" value="{{item.name}}" /></h1>
  </header>

  <section class="sheet-body" style="padding: 10px;">
    <label>Script de Efeito</label>
    <textarea name="system.origin_effect_script" style="width: 100%; height: 200px;">{{item.system.origin_effect_script}}</textarea>
  </section>
</form>
